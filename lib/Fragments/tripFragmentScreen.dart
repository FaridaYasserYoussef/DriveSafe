import 'package:flutter/material.dart';

class TripFragmentScreen extends StatelessWidget {
  const TripFragmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}